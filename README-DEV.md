## Development notes

Build the React admin app using the bundled wp-scripts setup:

Install dependencies (if not already):

```bash
npm install
```

Build once:

```bash
npm run build
```

Start a watch/dev server (hot reload):

```bash
npm start
```

The built output will be placed in `build/index.js` which the plugin enqueues.
